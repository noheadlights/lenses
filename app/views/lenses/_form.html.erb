<%= simple_form_for @lense, :html => { :class => 'form-horizontal' } do |f| %>
  <% if @lense.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lense.errors.count, "error") %> prohibited this lense from being saved:</h2>

      <ul>
      <% @lense.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


    <%= f.association :brand, label: "Brand" %>
    <%= f.association :lense_type,  :label_method => :lense_type, :value_method => :id, :include_blank => false %>
    <%= f.input :max_aperture, as: :string %>
    <%= f.input :min_aperture, label: "Minimum Aperture", as: :string %>
    <%= f.input :max_focal_length, label: "Maximum Focal Length", as: :string %>
    <%= f.input :min_focal_length, label: "Minimum Focal Length", as: :string %>
    <%= f.input :name, label: "Lens Name" %>
    <%= f.input :notes, label: "Notes" , as: :text%>
    <%= f.submit %>
  </div>
<% end %>
